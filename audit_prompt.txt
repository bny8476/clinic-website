<USER_REQUEST>
# Claude Code Prompt — Elixir Health Care: Fix Audit Findings

Paste everything below into Claude Code in the `clinic-website` repo root.

---

Act as a Senior Full-Stack Engineer and Security Engineer. Work through the
issues below **in the exact order listed** — later items depend on earlier
ones being done first (e.g. don't touch the duplicate models before secrets
are rotated, since you'll be editing the same files twice otherwise).

For every item: make the change, then run the relevant build/test command,
and report pass/fail before moving to the next item. Do not batch all
changes into one giant commit — commit after each numbered section with a
clear message. If a fix requires a decision only the project owner can make
(e.g. which of two duplicate classes is canonical), stop and ask instead of
guessing.

## 1. Secrets — rotate and stop committing `.env` (CRITICAL, do first)

- Confirm whether `.env` at the repo root is tracked by git: run
  `git log --all -- .env`. If it has ever been committed, treat
  `JWT_SECRET`, `SEED_ADMIN_PASSWORD`, and `SEED_DOCTOR_PASSWORD` in it as
  compromised.
- Generate a new `JWT_SECRET` with `openssl rand -hex 32` and new seed
  passwords. Put the new values only in a local, gitignored `.env` — never
  print them in chat or commit them.
- Verify `.env` is listed in `.gitignore` (it already is) and run
  `git rm --cached .env` if `git log` showed it was ever tracked, so history
  cleanup (e.g. `git filter-repo`) can be scheduled separately.
- In `backend/src/main/resources/application.yml`, remove the insecure
  inline fallback for `jwt.secret` and make startup fail fast if it's
  missing, mirroring the existing `DataSeeder.java` pattern that already
  refuses to boot on `CHANGE_ME_*` sentinels:
  ```yaml
  jwt:
    secret: ${JWT_SECRET:?JWT_SECRET environment variable must be set}
  ```
- In `docker-compose.yml`, replace the hardcoded `POSTGRES_PASSWORD: password`
  with `POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}` for consistency with every
  other secr
<truncated 7221 bytes>
clude="*.js"`).

**Verify:** `git status` is clean; the deleted patch files' changes are
confirmed present in the corresponding live source files (paste the diff
check output in your final report before deleting).

## 9. Docker/infra hardening (MEDIUM, do last — lowest code risk)

- Add `healthcheck:` blocks to `docker-compose.yml` for `postgres` (e.g.
  `pg_isready`) and `clinic-app` (hit `/actuator/health`), and make
  `clinic-app` and `clinic-frontend` use `depends_on: condition:
  service_healthy` instead of plain `depends_on`.
- Add a non-root `USER` instruction to both `backend/Dockerfile` and
  `frontend/Dockerfile` (create an app user in the final stage before
  `ENTRYPOINT`/`CMD`).

**Verify:** `docker compose up` — confirm containers report healthy status
and the app container is not running as root
(`docker exec <container> whoami`).

---

## Final report format

After completing all sections, produce a short summary table:

| # | Section | Status | Tests run | Notes |
|---|---|---|---|---|

Flag anything you skipped or need a decision on rather than guessing,
especially in Section 4 (duplicate model consolidation) and Section 1
(whether `.env` was ever actually committed to git history).
</USER_REQUEST>
<ADDITIONAL_METADATA>
The current local time is: 2026-08-03T10:31:34+05:30.

The user's current state is as follows:
Active Document: /Users/eakhalaivan/Downloads/clinic-website/frontend/src/pages/doctor/DoctorDashboard.jsx (LANGUAGE_JAVASCRIPT)
Cursor is on line: 538
Other open documents:
- /Users/eakhalaivan/Downloads/clinic-website/frontend/src/pages/doctor/DoctorDashboard.jsx (LANGUAGE_JAVASCRIPT)
- /Users/eakhalaivan/Downloads/clinic-website/frontend/src/pages/doctor/DoctorDashboard.css (LANGUAGE_CSS)
- /Users/eakhalaivan/Downloads/clinic-website/backend/target/generated-sources/annotations/com/healthcare/clinic/inventory/mapper/UserMapperImpl.java (LANGUAGE_JAVA)
- /Users/eakhalaivan/Downloads/clinic-website/frontend/src/pages/patient/PatientDashboard.css (LANGUAGE_CSS)
</ADDITIONAL_METADATA>